<Window x:Class="VirtualClassroom.AssignGradesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Assign Grades" Height="350" Width="450">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" 
                   Text="Select Student and Course to Assign Grade" 
                   FontWeight="Bold" Margin="0,0,0,15"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Student:" Margin="0,5"/>
        <ComboBox x:Name="CboStudents" Grid.Row="1" Grid.Column="1" 
                  Margin="5" DisplayMemberPath="StudentName"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Course:" Margin="0,5"/>
        <ComboBox x:Name="CboCourses" Grid.Row="2" Grid.Column="1" 
                  Margin="5" DisplayMemberPath="CourseName"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Grade (A-F):" Margin="0,5"/>
        <TextBox x:Name="TxtGrade" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                Content="Save Grade" 
                Click="SaveGrade_Click" 
                Margin="0,20,0,0" Padding="10"/>

    </Grid>
</Window>